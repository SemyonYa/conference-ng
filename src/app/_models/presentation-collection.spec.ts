import { PresentationCollection } from './presentation-collection';

describe('PresentationCollection', () => {
  it('should create an instance', () => {
    expect(new PresentationCollection()).toBeTruthy();
  });
});
